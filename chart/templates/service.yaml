{{- with (first .Values.spec.routing) }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $.Release.Name }}
spec:
  selector:
    app.kubernetes.io/name: {{ $.Release.Name }}
  ports:
    - port: {{ .port }}
      targetPort: {{ .port }}
{{- end }}
