# NATS + JetStream — events 어휘(publishes/consumes)의 기반. CR 해석은 NACK(nack/).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nats
  namespace: bee-substrate
  labels: { app.kubernetes.io/name: nats }
spec:
  replicas: 1
  selector:
    matchLabels: { app.kubernetes.io/name: nats }
  template:
    metadata:
      labels: { app.kubernetes.io/name: nats }
    spec:
      containers:
        - name: nats
          image: nats:2.10-alpine
          args: ["-js", "-m", "8222"]
          ports:
            - containerPort: 4222
            - containerPort: 8222
---
apiVersion: v1
kind: Service
metadata:
  name: nats
  namespace: bee-substrate
spec:
  selector: { app.kubernetes.io/name: nats }
  ports:
    - { name: client, port: 4222, targetPort: 4222 }
    - { name: monitor, port: 8222, targetPort: 8222 }
